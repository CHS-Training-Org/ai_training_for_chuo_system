# Application Design Plan: CSV 帳票出力

## 方針

コンポーネント境界・メソッドシグネチャ・サービス層のオーケストレーション・依存関係は、plan mode での調査（Explore エージェント2体・Plan エージェント2体）ですでに確定している。新規の確認質問は不要と判断した（既存の4層アーキテクチャ・既存のロールベース認可パターンにそのまま従うため、コンポーネント境界に曖昧な点がない）。

## 実行チェックリスト

- [ ] `components.md` を生成する
- [ ] `component-methods.md` を生成する
- [ ] `services.md` を生成する
- [ ] `component-dependency.md` を生成する
- [ ] `application-design.md`（統合版）を生成する
