{
  "name": "BookFlow Full Stack",
  "dockerComposeFile": "docker-compose.yml",
  "service": "frontend",
  "workspaceFolder": "/workspace",
  // Claude Code 未起動だとホストに ~/.claude.json が存在せず、Docker がマウント元を
  // 空ディレクトリとして自動生成 → ファイル↔ディレクトリの型衝突でコンテナ起動が失敗する。
  // マウント前にホスト側で空ファイル/ディレクトリの存在を保証して回避する（Windows ホスト前提）。
  "initializeCommand": "powershell -NoProfile -Command \"$j = Join-Path $env:USERPROFILE '.claude.json'; if (-not (Test-Path $j)) { Set-Content -Path $j -Value '{}' -NoNewline }; $d = Join-Path $env:USERPROFILE '.claude'; if (-not (Test-Path $d)) { New-Item -ItemType Directory -Path $d | Out-Null }\"",
  "features": {
    "ghcr.io/devcontainers/features/java:1": { "version": "25" },
    "ghcr.io/devcontainers/features/node:2.0.0": { "version": "24" },
    "ghcr.io/devcontainers/features/github-cli:1": {},
    "ghcr.io/anthropics/devcontainer-features/claude-code:1": {},
    "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {}
  },
  "mounts": [
    "source=${localEnv:USERPROFILE}/.claude,target=/root/.claude,type=bind,consistency=cached",
    "source=${localEnv:USERPROFILE}/.claude.json,target=/root/.claude.json,type=bind,consistency=cached"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        // Java 関連は Extension Pack for Java に集約。redhat.java / vscode-java-test /
        // vscode-gradle 等を内包するため、個別指定で起きる「依存先(redhat.java)が未インストール」
        // のインストール順序レースを回避できる。
        "vscjava.vscode-java-pack",
        "vmware.vscode-spring-boot",
        "bradlc.vscode-tailwindcss",
        "oxc.oxc-vscode",
        "ms-playwright.playwright",
        "ms-azuretools.vscode-docker",
        "Anthropic.claude-code"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "java.jdt.ls.java.home": "/usr/local/sdkman/candidates/java/current"
      }
    }
  },
  "forwardPorts": [3000, 8000],
  "containerEnv": {
    "WAYLAND_DISPLAY": "",
    "DISPLAY": ""
  },
  "postCreateCommand": "bash .devcontainer/postCreate.sh"
}
